# Visual Asset Pipeline plugin source

This is a skills-only portable plugin package, not a running MCP server and not an
already installed plugin. Follow the host's documented plugin installation/development
process. The complete skill is in `skills/visual-asset-pipeline`; start with its
`INSTALL.md` and `SKILL.md`. It uses existing authorized tools and includes no credentials
or automatic remote service connections. See the skill's test report for exact validation.
